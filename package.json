{
  "name": "join-tasks-fullstack",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix front\" \"npm run dev --prefix back\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
